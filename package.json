{
  "name": "clarion",
  "private": true,
  "version": "0.0.0",
  "description": "Clarion — React Native audio & speech for Android and iOS",
  "license": "MIT",
  "author": "anvesh-dev",
  "repository": { "type": "git", "url": "https://github.com/Th4nderG0d/clarion" },
  "scripts": {
    "build": "pnpm -r --filter './packages/*' run build",
    "typecheck": "pnpm -r --filter './packages/*' run typecheck",
    "clean": "pnpm -r run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.12.0"
}
