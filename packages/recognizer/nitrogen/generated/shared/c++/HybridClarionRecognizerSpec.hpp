///
/// HybridClarionRecognizerSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `NativeRecognizerConfig` to properly resolve imports.
namespace margelo::nitro::clarion::recognizer { struct NativeRecognizerConfig; }
// Forward declaration of `NativeTranscriptResult` to properly resolve imports.
namespace margelo::nitro::clarion::recognizer { struct NativeTranscriptResult; }
// Forward declaration of `NativeRecognizerError` to properly resolve imports.
namespace margelo::nitro::clarion::recognizer { struct NativeRecognizerError; }

#include <string>
#include <NitroModules/Promise.hpp>
#include <vector>
#include "NativeRecognizerConfig.hpp"
#include "NativeTranscriptResult.hpp"
#include <functional>
#include "NativeRecognizerError.hpp"

namespace margelo::nitro::clarion::recognizer {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `ClarionRecognizer`
   * Inherit this class to create instances of `HybridClarionRecognizerSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridClarionRecognizer: public HybridClarionRecognizerSpec {
   * public:
   *   HybridClarionRecognizer(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridClarionRecognizerSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridClarionRecognizerSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridClarionRecognizerSpec() override = default;

    public:
      // Properties
      virtual std::string getState() = 0;

    public:
      // Methods
      virtual std::shared_ptr<Promise<bool>> isAvailable(const std::string& language) = 0;
      virtual std::shared_ptr<Promise<std::vector<std::string>>> supportedLocales() = 0;
      virtual std::shared_ptr<Promise<void>> prepare(const NativeRecognizerConfig& config) = 0;
      virtual std::shared_ptr<Promise<void>> start() = 0;
      virtual std::shared_ptr<Promise<NativeTranscriptResult>> stop() = 0;
      virtual std::shared_ptr<Promise<void>> discard() = 0;
      virtual std::shared_ptr<Promise<void>> release() = 0;
      virtual double addStateListener(const std::function<void(const std::string& /* state */)>& listener) = 0;
      virtual double addAudioLevelListener(const std::function<void(double /* rms */, double /* peak */)>& listener) = 0;
      virtual double addPartialListener(const std::function<void(const NativeTranscriptResult& /* result */)>& listener) = 0;
      virtual double addFinalListener(const std::function<void(const NativeTranscriptResult& /* result */)>& listener) = 0;
      virtual double addErrorListener(const std::function<void(const NativeRecognizerError& /* error */)>& listener) = 0;
      virtual void removeListener(double id) = 0;
      virtual void removeAllListeners() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "ClarionRecognizer";
  };

} // namespace margelo::nitro::clarion::recognizer
