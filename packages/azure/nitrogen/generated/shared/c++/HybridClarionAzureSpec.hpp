///
/// HybridClarionAzureSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `NativeAzureConfig` to properly resolve imports.
namespace margelo::nitro::clarion::azure { struct NativeAzureConfig; }
// Forward declaration of `NativeTranscriptResult` to properly resolve imports.
namespace margelo::nitro::clarion::azure { struct NativeTranscriptResult; }
// Forward declaration of `NativeAzureError` to properly resolve imports.
namespace margelo::nitro::clarion::azure { struct NativeAzureError; }

#include <string>
#include <NitroModules/Promise.hpp>
#include "NativeAzureConfig.hpp"
#include "NativeTranscriptResult.hpp"
#include <functional>
#include "NativeAzureError.hpp"

namespace margelo::nitro::clarion::azure {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `ClarionAzure`
   * Inherit this class to create instances of `HybridClarionAzureSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridClarionAzure: public HybridClarionAzureSpec {
   * public:
   *   HybridClarionAzure(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridClarionAzureSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridClarionAzureSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridClarionAzureSpec() override = default;

    public:
      // Properties
      virtual std::string getState() = 0;

    public:
      // Methods
      virtual std::shared_ptr<Promise<bool>> isAvailable(const NativeAzureConfig& config) = 0;
      virtual std::shared_ptr<Promise<void>> prepare(const NativeAzureConfig& config) = 0;
      virtual std::shared_ptr<Promise<void>> start() = 0;
      virtual std::shared_ptr<Promise<NativeTranscriptResult>> stop() = 0;
      virtual std::shared_ptr<Promise<void>> discard() = 0;
      virtual std::shared_ptr<Promise<void>> release() = 0;
      virtual std::shared_ptr<Promise<void>> updateAuthToken(const std::string& token) = 0;
      virtual double addStateListener(const std::function<void(const std::string& /* state */)>& listener) = 0;
      virtual double addAudioLevelListener(const std::function<void(double /* rms */, double /* peak */)>& listener) = 0;
      virtual double addPartialListener(const std::function<void(const NativeTranscriptResult& /* result */)>& listener) = 0;
      virtual double addFinalListener(const std::function<void(const NativeTranscriptResult& /* result */)>& listener) = 0;
      virtual double addErrorListener(const std::function<void(const NativeAzureError& /* error */)>& listener) = 0;
      virtual double addSpeechBoundaryListener(const std::function<void(const std::string& /* kind */, double /* offsetMs */)>& listener) = 0;
      virtual void removeListener(double id) = 0;
      virtual void removeAllListeners() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "ClarionAzure";
  };

} // namespace margelo::nitro::clarion::azure
