///
/// HybridClarionAudioTapSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `NativeAudioTapFormat` to properly resolve imports.
namespace margelo::nitro::clarion::audio_tap { struct NativeAudioTapFormat; }
// Forward declaration of `NativeAudioTapFrame` to properly resolve imports.
namespace margelo::nitro::clarion::audio_tap { struct NativeAudioTapFrame; }
// Forward declaration of `NativeAudioTapStats` to properly resolve imports.
namespace margelo::nitro::clarion::audio_tap { struct NativeAudioTapStats; }
// Forward declaration of `NativeAudioTapError` to properly resolve imports.
namespace margelo::nitro::clarion::audio_tap { struct NativeAudioTapError; }

#include <string>
#include <NitroModules/Promise.hpp>
#include "NativeAudioTapFormat.hpp"
#include "NativeAudioTapFrame.hpp"
#include <functional>
#include "NativeAudioTapStats.hpp"
#include "NativeAudioTapError.hpp"

namespace margelo::nitro::clarion::audio_tap {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `ClarionAudioTap`
   * Inherit this class to create instances of `HybridClarionAudioTapSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridClarionAudioTap: public HybridClarionAudioTapSpec {
   * public:
   *   HybridClarionAudioTap(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridClarionAudioTapSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridClarionAudioTapSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridClarionAudioTapSpec() override = default;

    public:
      // Properties
      virtual std::string getState() = 0;
      virtual double getListenerCount() = 0;

    public:
      // Methods
      virtual std::shared_ptr<Promise<void>> start(const NativeAudioTapFormat& format) = 0;
      virtual std::shared_ptr<Promise<void>> stop() = 0;
      virtual std::shared_ptr<Promise<void>> release() = 0;
      virtual double addFrameListener(const std::function<void(const NativeAudioTapFrame& /* frame */)>& listener) = 0;
      virtual double addStateListener(const std::function<void(const std::string& /* state */)>& listener) = 0;
      virtual double addStatsListener(const std::function<void(const NativeAudioTapStats& /* stats */)>& listener) = 0;
      virtual double addErrorListener(const std::function<void(const NativeAudioTapError& /* error */)>& listener) = 0;
      virtual void removeListener(double id) = 0;
      virtual void removeAllListeners() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "ClarionAudioTap";
  };

} // namespace margelo::nitro::clarion::audio_tap
